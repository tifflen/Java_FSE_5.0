# SingletonPatternExample

## Files
- `Logger.java` - Singleton implementation
- `SingletonTest.java` - verifies only one instance is created/used

## Compile & Run (no build tool required)
From the `SingletonPatternExample` folder:

```bat
javac Logger.java SingletonTest.java
java SingletonTest
```

You should see a confirmation message that the test passed.

